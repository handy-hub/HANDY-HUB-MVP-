# Future Web UI Layer

Use this folder for cross-platform web UI controllers for future expansion.
Keep backend logic out of this layer by using repositories/domain services through the DI container.

