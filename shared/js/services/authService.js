// Legacy entrypoint kept for backward compatibility with existing signup.html script tags.
// UI logic now lives in the UI layer and backend access is routed through domain/repositories.
import "../utils/global-app.js";
import "../ui/customer/signupPageController.js";

