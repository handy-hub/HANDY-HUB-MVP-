# HandyHub MVP

This repository has been restructured to match the requested target layout.

- `shared/` contains shared CSS, JS, and assets.
- `customer-app/` contains the customer-facing web pages. 
- `artisan-app/` contains the artisan-facing pages.
- `functions/` contains Firebase Cloud Function stubs.
