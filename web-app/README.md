# Future Web App Placeholder

This folder is reserved for the future standalone web version.
Shared architecture is already prepared in `shared/js/app`, `shared/js/data`, `shared/js/domain`, and `shared/js/backend`.

