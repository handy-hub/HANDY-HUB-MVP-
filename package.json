{
  "name": "handyhub-mvp",
  "version": "1.0.0",
  "description": "Restructured HandyHub MVP project",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project lamax-rules-test \"node tests/firestore-rules.test.mjs\"",
    "dev": "npx serve . -p 8766 --no-clipboard",
    "start": "npx serve . -p 8766 --no-clipboard",
    "serve:ca": "npx serve customer-app -p 8765 --no-clipboard",
    "stage": "node scripts/stage-hosting.cjs customer-app && node scripts/stage-hosting.cjs artisan-app",
    "deploy": "npm run lint:filters && npm run stage && firebase deploy --only hosting",
    "deploy:ca": "npm run lint:filters && node scripts/stage-hosting.cjs customer-app && firebase deploy --only hosting:customer",
    "deploy:aa": "npm run lint:filters && node scripts/stage-hosting.cjs artisan-app && firebase deploy --only hosting:artisan",
    "deploy:rules": "firebase deploy --only firestore:rules,storage",
    "deploy:all": "npm run lint:filters && npm run stage && firebase deploy",
    "lint:filters": "node scripts/check-filter-tabs.cjs",
    "lint:tokens": "node scripts/design-token-lint.cjs",
    "lint:tokens:strict": "node scripts/design-token-lint.cjs --strict",
    "lint:status": "node scripts/check-status-vocab.cjs",
    "lint:instant": "node scripts/check-instant-ux.cjs",
    "lint:syntax": "node scripts/check-syntax.cjs",
    "lint": "npm run lint:syntax && npm run lint:filters && npm run lint:tokens && npm run lint:status && npm run lint:instant",
    "test:settings-overlays": "node tests/settings-overlay-architecture.test.cjs",
    "test:transaction-receipt": "node tests/transaction-receipt-modal.test.cjs",
    "test:topup-charge": "node tests/topup-charge.test.cjs",
    "test": "npm run lint && npm run test:settings-overlays && npm run test:transaction-receipt && npm run test:topup-charge && python tests/verify_low_fixes.py"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "serve": "^14.0.0"
  }
}
